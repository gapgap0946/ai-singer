# ===== AI 翻唱智能体依赖 =====
# 注意：PyTorch 需单独按 CUDA 版本安装，见 README：
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121

# 人声/伴奏分离
demucs>=4.0.1

# 歌声转换 (RVC)
# 注意：rvc-python 0.1.5 依赖已停止维护的 fairseq==0.12.2，在 Windows/Py3.10 无法安装。
# 歌声转换环节请二选一（见 README「歌声转换方案」）：
#   方案A：单独部署 RVC WebUI 整合包，本项目通过命令行调用
#   方案B：改用 so-vits-svc-fork（纯 pip 可装，不依赖 fairseq）
# so-vits-svc-fork>=4.1.61

# 音频处理
librosa>=0.10.1
soundfile>=0.12.1
pydub>=0.25.1
numpy>=1.23,<2.0
scipy>=1.11.0

# Web 后端
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# 从 HuggingFace 下载模型
huggingface-hub>=0.23.0

# 工具
pydantic>=2.6.0
tqdm>=4.66.0
